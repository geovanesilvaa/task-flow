import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    height: 55,
    backgroundColor: "#fff",
  },
  text: {
    fontSize: 18,
    marginTop: 15,
    marginLeft: 15,
    textAlign: "left",
    fontWeight: "bold",
  },
});

export default styles;
